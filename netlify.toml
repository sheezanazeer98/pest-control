[build]
  command = "npm run build"
  publish = "."

[build.environment]
  NODE_VERSION = "20"

# Note: Netlify Forms are automatically detected from the hidden
# <form name="contact" data-netlify="true"> element in index.html.
# Submissions will appear in the Netlify dashboard under Forms.
# SPA routing is handled by the _redirects file.
